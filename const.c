#include <stdio.h>

int main() {
  const int A = 15; // It's good practise to write constant names uppercase.
  const float B = 20.99;
  const double C = 63.7953;
  const char D = 'a';
  
  printf("Integer: %lu\n", sizeof(a));
  printf("Float: %lu\n", sizeof(b));
  printf("Double: %lu\n", sizeof(c));
  printf("Char: %lu\n", sizeof(d));
}
